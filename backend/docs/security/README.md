# Security documentation

Security dossiers and platform security references are kept in the parent
`docs` directory. Repository security policy and reporting instructions are in
the root [`SECURITY.md`](../../SECURITY.md).
