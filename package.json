{
  "name": "procureflow",
  "version": "5.0.0",
  "private": true,
  "description": "ProcureFlow — hotel-group procurement on Zoho Catalyst. React client in frontend/, API in functions/procurement_api.",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev",
    "dev:catalyst": "node verification/devserver.mjs",
    "verify": "bash verification/verify.sh",
    "deploy:api": "catalyst deploy --only functions:procurement_api",
    "deploy:gate": "catalyst deploy --only functions:procurement_signup_gate",
    "deploy:client": "npm run build && catalyst deploy --only client"
  }
}
